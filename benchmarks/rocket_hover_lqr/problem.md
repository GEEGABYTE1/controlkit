# Rocket Hover LQR

A small deterministic hover linearization with state `[x, z, vx, vz, theta, theta_dot]` and
control `[thrust_delta, torque]`. This is a compiler/control-stack benchmark, not a flight model.
